# Copier en .env et renseigner les vraies valeurs. Ne jamais committer .env.

# Local : Postgres via pgAdmin. Prod : Neon (voir .env.production, non committé).
DATABASE_URL="postgresql://postgres:CHANGEME@localhost:5432/afriflow?schema=public"

# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="CHANGEME"
NEXTAUTH_URL="http://localhost:3000"
